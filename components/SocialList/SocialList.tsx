import { SocialIconProps, networkFor } from "react-social-icons"

type SocialItemType = {
  url?: string
  href?: string
  network: string
  bgColor?: string
}
const socialList: SocialIconProps[] = [
  {

  }
]

export const SocialList = () => {
  return (
    <div>
      {

      }
    </div>
  )
}